# Dynamic List in React

This is a simple React application that renders a hardcoded list of three items: "React", "JavaScript", "CSS".

## How to Run

1. Install dependencies:
   ```
   npm install
   ```
2. Start the application:
   ```
   npm start
   ```
3. Open your browser and visit `http://localhost:3000`